---
name: forge:scope
description: Scope a project or enhancement — gather requirements, explore codebase, produce forge/scope.md
argument-hint: "<description of what to build>"
---

Create `forge/` directory if needed. Delegate to the **scoper** agent with the user's request. The scoper gathers requirements interactively and produces `forge/scope.md` and `forge/config.md`.
